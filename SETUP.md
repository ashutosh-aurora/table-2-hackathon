# Setup

Goal: every person at the table can run Claude Code with this folder open. If that is not possible
for someone, the facilitator runs it and the table works through them.

## Steps

1. **Install / open Claude Code.** `[Confirm the exact access route for offsite attendees here —
   desktop app, IDE extension, or web. To be filled once IT confirms.]`
2. **Get this folder.** Clone the kit repository, or open the shared copy provided.
   `[Repository URL / shared location to be filled.]`
3. **Open the folder in Claude Code.** It will read `CLAUDE.md` automatically. Ask Claude "what is
   this folder?" to confirm it has the context. **Humans: open `CLAUDE.md` and read it too — it's the
   journey you and the agent both follow.**
4. **Check the skills are visible.** Ask Claude to list available skills; you should see
   `feature-spec`, `grill-me-with-docs`, `shape-to-slice`, `prototype-kickoff`.

## If setup fails for someone

The facilitator is the guaranteed floor. If an attendee cannot get Claude Code working, they pair
with someone who can, or with the facilitator. No table should lose time to one person's setup.

## Access checklist (facilitator, before the day)

- [ ] Every attendee has a working Claude Code login
- [ ] The kit folder is available to clone or copy
- [ ] You have run the full arc once on the worked example (the dry-run)
